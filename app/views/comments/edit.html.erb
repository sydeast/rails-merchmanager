<div>
    <% if @comment.post %>
        <h1>New Comment for <%= @comment.post.title.capitalize %></h1>
    <% else %>
        <h1>New Comment </h1>
    <% end %>
</div>
<%= link_to 'Back' , :back %>


<div>
    <%= render 'comments/form' %>

</div>