<h1>All Comments</h1>

<div>
    <ul>
        <% @comments.each do |c| %>
            <li><%= c.user.name%> said "<%=c.content%>" published <%= time_ago_in_words(@post.created_at) %> about this post: <%=link_to c.post.title, post_path(c.post_id) %></li>
            <% end %>
    </ul>

</div>